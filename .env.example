# Production Mode (set to 'production' for production builds)
# This is optional - Vite automatically detects production mode during build
VITE_PRODUCTION=development

# Backend API Base URL (optional - defaults to localhost:3000 in dev, production URL in prod)
# Uncomment and set if you need to override the default
VITE_API_BASE_URL=http://localhost:3000/api/movies
